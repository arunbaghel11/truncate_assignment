Frontend (React + Vite)
-----------------------

1. Install dependencies: `cd frontend && npm install`
2. Start dev server: `npm run dev`
3. Configure backend base URL in `frontend/src/services/api.js` if backend runs elsewhere.
